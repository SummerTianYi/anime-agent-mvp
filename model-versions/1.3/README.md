# 1.3 冻结与动捕交接 — Codex，2026-09-08

本版由所有者看过对比图后批准，运行时提交为 `581a856c51b1de4387896ce5ae283e279ff67d22`。它收口恢复原姿势以来的三项工作：待机肩臂链采用旧斜下外展 A-pose（仍保留动捕头胸运动）；十个材质采用已批准的候选 A 参数，提升明暗层次；F12 原生渲染高清透明 PNG。没有改官方模型、身体比例、纹理或骨架，也没有加入前次物理专项的求解器。完整清单、哈希、预览和回滚命令见同目录 manifest.json，贡献边界见 docs/CONTRIBUTIONS.md。

| 项目 | 实测结果与边界 |
|---|---|
| 渲染与结构 | 九组同帧视图/表情及十二个动画时刻/朝向组合通过；采样 Alpha 差异 0，完整轮廓框相同，703 运行骨姿态/相机/FOV/网格边界不变；23 表面、48 表情、两条完整 17 骨辫子；GLB 和 Blend 均与 1.2 同哈希。 |
| 可回退 | 1.1、1.2、1.3、空值与旧别名测试通过，回退 RGB 平均误差 <0.000001。材质切换不恢复历史动作，完整回溯请用 open-model-version.ps1 的隔离 worktree。 |
| 图像与性能 | 五视角真实高清输出含最大缩放，2880×3480、透明、无 UI；忙碌/非法请求拒绝及临时资源释放通过。单图约 284–330ms，非连续录像。单个 1920×2320 3D Viewport 两轮 GPU P95 约 0.343ms；不是桌面端到端帧率。 |
| 回归 | 保守待机 70 骨/750 帧、动作 runtime/asset、文字输入、会话 UI、画布、大动作 safety 均通过；TypeScript/Vite 构建通过；从提交导出的干净目录运行 181 项 Core 单测及文档守卫通过。没有夹带另一代理未提交的唤醒词实现/测试。 |
| 当前会话 | Core、Avatar 桥接与 TTS 在只读检查中健康。尝试仅重启本项目 Avatar 的命令被执行策略拦截，未执行；不声称已重启实装窗口。磁盘默认已是 1.3，下次正常启动生效；本轮实际渲染测试使用同一个生产材质切换 API。 |
| 尚未解决 | 全身碰撞、贴腿待机穿裙、复杂旋转裙装自然性、连续高清录制、自适应画质；本版不证明这些能力成立。未再启动子代理，交接采用单代理审查与自动门禁。 |

本机仓库根目录：`D:/UserData/Administrator/Documents/Codex/2026-08-28/https-github-com-summertianyi-anime-agent/work/anime-agent-mvp`。不可再分发的 GLB、可编辑 Blend 与七张预览存放在其同级 `model-archive/1.3/`，代码和清单上传 GitHub，资产保持本地只读副本。完整同帧对比及 report.json 位于 `C:/Users/26052/AppData/Roaming/Godot/app_userdata/Luo Tianyi Desktop Avatar MVP/lookdev-1.3-candidate/2026-09-08T22-53-16-53124/`，正面/面部对比分别为 ab-front.png、ab-face.png。1.0/1.1/1.2 档案不覆盖；核验入口为 `scripts/model-versions/verify-model-versions.ps1`。

下一阶段只做小幅、少接触动捕，先点头、左右看、轻微重心移动，沿用现有 A-pose 与 1.3 材质，不自动重开全身物理工程。新动作先检查原始视频全程关节可见性，再提取、重定向到官方骨架、循环/平滑检查、全轨迹多视角渲染，最终由所有者验收。两条长辫子仍完整归入上半身层；手臂需主动留出裙装/躯干/辫子的间隙，小幅度不等于自动不会穿模。先读 HANDOFF 与 MOTION_PIPELINE，检查工作树归属，不覆盖其他代理改动。
